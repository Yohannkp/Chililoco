<form method="post" action="{{ path('SupprimerPlat', {'id': plat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plat.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
